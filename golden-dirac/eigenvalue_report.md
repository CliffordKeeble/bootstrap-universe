# Golden Dirac — Eigenvalue Report

Generated: 2026-04-11  
By: Mr Code  
Programme: Bootstrap Universe / 2I Universe  
Dr Clifford Keeble, Woodbridge UK, ORCID 0009-0003-6828-2155

---

## 1. Individual gamma matrix eigenvalues

All three gamma matrices have eigenvalues at the fourth root of 5.

| Matrix | Eigenvalues | Multiplicity | Type |
|--------|-------------|-------------|------|
| G1 = K ⊗ Γ_seed | ±5^(1/4) | 2 each | **Real** |
| G2 = J ⊗ Γ_seed | ±5^(1/4)·i | 2 each | **Imaginary** |
| G3 = I ⊗ Γ_adj  | ±5^(1/4)·i | 2 each | **Imaginary** |

Numerical value: 5^(1/4) = 1.49534878

The Planck scale is one level deeper than √5. The fundamental
eigenvalue is the fourth root.

## 2. Proxy Dirac operator spectrum

D_proxy = G1 + G2 + G3

| Eigenvalue | Multiplicity |
|-----------|-------------|
| +5^(1/4)·i | 2 |
| −5^(1/4)·i | 2 |

**Minimum nonzero |λ| = 5^(1/4) = 1.49534878**

This is the Planck candidate eigenvalue. The free golden Dirac
operator has a single frequency mode at the fourth root of 5.

## 3. Chirality operator Γ⁵

Γ⁵ = G1·G2·G3

Matrix:
```
[[ 0,  -√5,  0,   0 ],
 [ 5,   0,   0,   0 ],
 [ 0,   0,   0,   √5],
 [ 0,   0,  -5,   0 ]]
```

Eigenvalues: ±5^(3/4)·i  (multiplicity 2 each)

**(Γ⁵)² = −5√5 · I₄ = −5^(3/2) · I₄**

The chirality operator is itself golden. Its square is not ±I
(as in standard Dirac) but −5^(3/2)·I₄. Chirality carries the
golden norm. The three-halves power: √5 × 5 = 5^(3/2).

This is the cube of the seed norm composed with the sign flip.
Handedness is not Boolean in this algebra — it is scaled by the
golden cascade: 5^(1/4) → 5^(1/2) → 5^(3/4) → 5^(3/2).

## 4. Seed vs Adjoint chirality

**Anticommutator: {Γ_seed, Γ_adj} = 0** — confirmed.

**Commutator:**
```
[Γ_seed, Γ_adj] = [[2√5, 0], [0, -2√5]] = 2√5 · σ₃
```

The commutator is diagonal, proportional to √5. The seed and
adjoint do not commute — their commutator points along the
z-axis of the golden algebra.

### The big result

| Matrix | Eigenvalues | Type |
|--------|-------------|------|
| Γ_seed | ±5^(1/4) | **Purely real** |
| Γ_adj  | ±5^(1/4)·i | **Purely imaginary** |

**i emerges from the golden split.**

Γ_seed and Γ_adj differ by a single sign. That sign flip rotates
the eigenvalues from the real axis to the imaginary axis. The
imaginary unit is not an input to the algebra — it is produced by
traversing the golden split in the opposite direction.

### Product structure

```
Γ_seed · Γ_adj = +√5 · σ₃ = [[ √5, 0], [0, -√5]]
Γ_adj · Γ_seed = −√5 · σ₃ = [[-√5, 0], [0,  √5]]
```

The product of the chiral partners is ±√5 times the diagonal
Pauli matrix. The golden ratio norm appears as the coupling
constant between the two chiralities.

## 5. Notes

Three results confirmed:

1. **5^(1/4) is the fundamental eigenvalue.** Every gamma matrix,
   and the proxy Dirac operator, has eigenvalues at ±5^(1/4)
   (real or imaginary). The Planck scale sits at the fourth root
   of 5, one level below √5.

2. **(Γ⁵)² = −5^(3/2)·I₄.** The chirality operator is golden.
   Its square is not ±I but carries the full golden norm cascade.
   Handedness is a golden quantity, not a binary one.

3. **i is a consequence, not an assumption.** Γ_seed has real
   eigenvalues ±5^(1/4). Γ_adj — which differs by one sign —
   has imaginary eigenvalues ±i·5^(1/4). Complex numbers emerge
   from the chiral structure of the golden split. Physics did not
   need to assume them.

The wrong square root paper writes itself from result 3.

---

*Generated by eigenvalue_analysis.py*  
*Golden Dirac operator — gamma matrices over Z[phi]*  
*Bootstrap Universe Programme*
